# Products

Product implementation lives here, separate from the GitHub Pages website at the repository root.

| Product | Contents |
| --- | --- |
| `git-magager/` | Chrome extension source in `extension/` and packaged releases in `releases/`. |
| `design-guideline-illustration-generator/` | Agent and skills for generating component documentation and illustrations. |

Codex plugin packages remain in the root-level `plugins/` directory so existing installation links stay stable.
