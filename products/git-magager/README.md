# Git Magager

The maintained source for the Git Magager Chrome extension.

- `extension/` — unpacked extension source; load this directory in Chrome for local development.
- `releases/` — packaged release archives.

The public product page and privacy policy live in the repository root at `git-magager/`.
